# Tree store: partial storage only
stores:
  - partial:
      mappers[clients]:
        attributes.logo:
          template: ${LOGO}
          read-only: true
        clientId: ${NAME}
        id: ${NAME}
        enabled: ${ENABLED}
        attributes.origin: partial
        attributes.some-info: ${NAME} is ${ENABLED}
      read-only[clients]: true

      contents[clients]:
        - NAME: client1
          ENABLED: false

  - partial:
      mappers[clients]:
        attributes.logo:
          template: ${LOGO}
          read-only: true
        clientId: ${NAME}
        id: ${NAME}
        enabled: ${ENABLED}
        attributes.origin: partial
        attributes.some-info: ${NAME} is ${ENABLED}
      read-only[clients]: true

      contents[clients]:
        - NAME: client2
          ENABLED: true
          LOGO: AQIDBAUGBwgJCgsMDQ4P

  - partial:
      mappers[clients]:
        attributes.logo:
          template: ${LOGO}
          read-only: true
        clientId: ${NAME}
        id: ${NAME}
        enabled: ${ENABLED}
        attributes.origin: partial
        attributes.some-info: ${NAME} is ${ENABLED}
      read-only[clients]: true

      contents[clients]:
        - NAME: client3
          ENABLED: true
          LOGO: null


